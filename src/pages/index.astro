---
export async function get({ redirect }) {
  // Redirige al usuario a la página en inglés
  return redirect('/portfolio/en', 301); // 301 indica redirección permanente
}
---

<Fragment>
  TEST
</Fragment>
