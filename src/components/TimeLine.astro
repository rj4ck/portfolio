---
interface Props {
  date: string;
  title: string;
  subtitle: string;
  startDate?: string;
  endDate?: string;
  location: string;
  showTimeline?: boolean;
}

const { date, title, subtitle, startDate, endDate, location, showTimeline = true } = Astro.props;
---

<div class="relative mt-2 pb-0">
  <div class="md:flex items-center md:space-x-4 mb-0">
    {
      showTimeline && (
        <div class="flex items-center space-x-4 md:space-x-2 md:space-x-reverse">
          <div class="flex items-center justify-center w-10 h-10 rounded-full bg-gray-800 border border-gray-700 shadow md:order-1">
            <slot name="icon" />
          </div>
          <time class="font-caveat font-medium text-sm text-orange-500 md:w-28">{date}</time>
        </div>
      )
    }

    <div class={showTimeline ? "ml-14" : ""}>
      <p class="text-xl">{subtitle}</p>
      <p class="text-orange-400">{title}</p>
      {
        !showTimeline && (
          <div>
            <span class="mr-6">
              <i class="fa-solid fa-calendar-days text-[10px]" /> {startDate} -{" "}
              {endDate ?? "Present"}
            </span>
            <span>
              <i class="fa-solid fa-location-dot text-[10px]" /> {location}
            </span>
          </div>
        )
      }
    </div>
  </div>

  <div
    class={`p-2 ${showTimeline ? "bg-gray-800 text-gray-400 rounded-md border border-gray-700 shadow ml-14 md:ml-44" : ""}`}
  >
    <slot />
  </div>
</div>
